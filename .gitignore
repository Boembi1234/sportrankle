# Build outputs (python build.py regenerates them)
sportrankle.html
dist/
img/
img-cache/

# Python
__pycache__/
*.pyc

# Netlify site link (per machine)
.netlify/

# Supabase: the public key of the maintainers' project (forks run without the online comparison)
supabase/anon.key
supabase/.temp/

# OS / editor
.DS_Store
Thumbs.db

# Local helpers (image generation with an API key outside the repo)
tools/
